-----------------------------------------------------------------------------------------------------------------------
First connect to your own database under: StopWords.java

Line 20: write database name
Line 21: Write connection String
Line 22: write password
Line 23: Write database user

-----------------------------------------------------------------------------------------------------------------------
The application contains a file "StopWordsEng.csv" this needs to be loaded into your database.
Here is the instruction to upload the file:

OBS: do NOT import the csv file before creating the columns.

1. Create a DB with intellij - OBS: if you have a database, add a table and go to step 2.

2. Create a table with two columns:

    nr - need specification (auto inc , not null ,int , unique , pk)
    name - need specification (text , unique , not null)

3. click on the table ---> import files ---> and then table is ready


-----------------------------------------------------------------------------------------------------------------------
under the IStopWords-interface you can see the methods available to use



-----------------------------------------------------------------------------------------------------------------------