package dk.sdu.se_f22.sortingmodule.range;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import java.util.HashMap;
import java.util.Map;

import static org.junit.jupiter.api.Assertions.*;

class HelpersTest {
    // Test if the helper creates the list of products as expected
    // Will be easier if split into 2 where the method for creating the list from a list of strings is tested first
    // Then the read from file can be tested using that first method
}
